{
  "name": "n09-minilab-postgres",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "jest --runInBand db-tests.test.js",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down -v",
    "db:perf": "docker compose exec -T postgres psql -U postgres -d eshop_test -f /workspace/performance.sql"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "supertest": "^7.1.4"
  },
  "devDependencies": {
    "jest": "^30.0.5"
  }
}
