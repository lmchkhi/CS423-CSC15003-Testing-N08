### Kiểm toán Test Case (Test Case Audit)

- **API Details:** Endpoint `{endpoint}`, Pool `{pool}`, Feature `{feature}`

#### Tóm tắt kiểm toán (Audit Summary)
| Test Case ID | Description | Category (Domain/State/Security/Schema) | AI Verdict | Human Verdict (VALID/INVALID/INCOMPLETE) | Reasoning | Corrections |
|--------------|-------------|-----------------------------------------|------------|------------------------------------------|-----------|-------------|
| `{id}` | `{desc}` | `{category}` | `{ai_verdict}` | `{human_verdict}` | `{reasoning}` | `{corrections}` |

#### Thống kê (Statistics)
- **Total reviewed:** `{total}`
- **Valid count:** `{valid}`
- **Invalid count:** `{invalid}`
- **Incomplete count:** `{incomplete}`

#### Các khu vực bao phủ còn thiếu (Missing coverage areas identified)
{missing_coverage_areas}

#### Test Cases mở rộng (Extended test cases - 5+ per API)
| Test Case ID | Lý do AI bỏ lỡ (Reasoning why AI missed them) |
|--------------|-----------------------------------------------|
| `{ext_id_1}` | `{reason}` |
| `{ext_id_2}` | `{reason}` |
| `{ext_id_3}` | `{reason}` |
| `{ext_id_4}` | `{reason}` |
| `{ext_id_5}` | `{reason}` |
