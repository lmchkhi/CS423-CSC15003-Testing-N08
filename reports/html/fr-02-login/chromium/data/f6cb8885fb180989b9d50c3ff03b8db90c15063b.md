# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: fr-02-login.spec.ts >> FR-02 — Đăng nhập & khóa tài khoản >> F02-TC-012 — Response dang nhap khong duoc chua mat khau nguoi dung
- Location: tests/fr-02-login.spec.ts:48:9

# Error details

```
Error: the login response must return a JWT, not the stored account record

expect(received).not.toContain(expected) // indexOf

Expected substring: not "\"password\""
Received string:        "{\"message\":\"Login successful\",\"token\":\"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpZCI6MjIsInJvbGUiOiJ1c2VyIiwiaWF0IjoxNzg1OTUwODQ1fQ.RZHWlSevdnwrJSL5hdp72dZDRByw85PXkwI-4aTPM6A\",\"user\":{\"id\":22,\"name\":\"HW04 Automation\",\"email\":\"fr02-1785950845524-574@eshop.test\",\"password\":\"Hw04Test!23\",\"role\":\"user\",\"login_attempts\":0,\"locked_until\":null,\"reset_token\":null,\"shipping_address\":null,\"phone\":null}}"
```

# Page snapshot

```yaml
- generic [ref=e3]:
  - banner [ref=e4]:
    - link "EShop" [ref=e5] [cursor=pointer]:
      - /url: /
    - navigation [ref=e6]:
      - link "Giỏ hàng" [ref=e7] [cursor=pointer]:
        - /url: /cart
      - generic [ref=e8]:
        - link "Chào, HW04 Automation" [ref=e9] [cursor=pointer]:
          - /url: /profile
        - button "Thoát" [ref=e10] [cursor=pointer]
  - main [ref=e11]:
    - generic [ref=e12]:
      - generic [ref=e13]:
        - heading "Danh sách sản phẩm" [level=1] [ref=e14]
        - generic [ref=e15]:
          - textbox "Tìm kiếm..." [ref=e16]
          - button "Tìm" [ref=e17] [cursor=pointer]
      - generic [ref=e18]:
        - generic [ref=e19]:
          - heading "iPhone 15 Pro Max" [level=2] [ref=e20]
          - paragraph [ref=e21]: 30,000,000 VND
          - generic [ref=e22]:
            - link "Xem chi tiết" [ref=e23] [cursor=pointer]:
              - /url: /product/1
            - button "Thêm vào giỏ" [ref=e24] [cursor=pointer]
        - generic [ref=e25]:
          - heading "Samsung Galaxy S24 Ultra" [level=2] [ref=e26]
          - paragraph [ref=e27]: 28,000,000 VND
          - generic [ref=e28]:
            - link "Xem chi tiết" [ref=e29] [cursor=pointer]:
              - /url: /product/2
            - button "Thêm vào giỏ" [ref=e30] [cursor=pointer]
        - generic [ref=e31]:
          - heading "MacBook Pro M3" [level=2] [ref=e32]
          - paragraph [ref=e33]: 45,000,000 VND
          - generic [ref=e34]:
            - link "Xem chi tiết" [ref=e35] [cursor=pointer]:
              - /url: /product/3
            - button "Thêm vào giỏ" [ref=e36] [cursor=pointer]
        - generic [ref=e37]:
          - heading "Tai nghe AirPods Pro 2" [level=2] [ref=e38]
          - paragraph [ref=e39]: 6,000,000 VND
          - generic [ref=e40]:
            - link "Xem chi tiết" [ref=e41] [cursor=pointer]:
              - /url: /product/4
            - button "Thêm vào giỏ" [ref=e42] [cursor=pointer]
        - generic [ref=e43]:
          - heading "Bàn phím cơ Keychron Q1" [level=2] [ref=e44]
          - paragraph [ref=e45]: 4,000,000 VND
          - generic [ref=e46]:
            - link "Xem chi tiết" [ref=e47] [cursor=pointer]:
              - /url: /product/5
            - button "Thêm vào giỏ" [ref=e48] [cursor=pointer]
      - heading "Hiển thị 5 sản phẩm" [level=1] [ref=e49]
  - contentinfo [ref=e50]: © 2026 EShop SUT. Dành cho mục đích kiểm thử.
```

# Test source

```ts
  38  | }
  39  | 
  40  | function resolveEmail(testCase: LoginCase, identity: Identity): string {
  41  |   if (testCase.emailSource === 'blank') return '';
  42  |   if (testCase.emailSource === 'literal') return testCase.emailLiteral ?? '';
  43  |   return identity.email;
  44  | }
  45  | 
  46  | test.describe('FR-02 — Đăng nhập & khóa tài khoản', () => {
  47  |   for (const testCase of cases) {
  48  |     test(`${testCase.caseId} — ${testCase.title}`, async ({ page }) => {
  49  |       const identity = await resolveIdentity(testCase);
  50  |       const email = resolveEmail(testCase, identity);
  51  |       const password =
  52  |         testCase.password === '@correct'
  53  |           ? identity.correctPassword
  54  |           : testCase.password;
  55  | 
  56  |       const loginPage = new LoginPage(page);
  57  |       await loginPage.goto();
  58  | 
  59  |       // Drive the consecutive-failure counter to this case's starting point.
  60  |       for (let attempt = 0; attempt < testCase.priorFailures; attempt += 1) {
  61  |         await loginPage.attemptLogin(identity.email, 'DefinitelyWrong!9');
  62  |         await expect(loginPage.errorMessage).toBeVisible();
  63  |       }
  64  | 
  65  |       if (testCase.waitSeconds > 0) {
  66  |         // A real wall-clock wait. FR-02's window is defined in seconds and
  67  |         // cannot be shortened without testing a different rule.
  68  |         await page.waitForTimeout(testCase.waitSeconds * 1000);
  69  |       }
  70  | 
  71  |       // The response-inspecting case must be listening before it submits.
  72  |       const loginResponse =
  73  |         testCase.assertion === 'response-excludes-password'
  74  |           ? page.waitForResponse((r) => r.url().includes('/api/login'))
  75  |           : null;
  76  | 
  77  |       await loginPage.fillCredentials(email, password);
  78  |       await loginPage.submit();
  79  | 
  80  |       switch (testCase.assertion) {
  81  |         case 'login-succeeds': {
  82  |           // Pattern 1 — navigation.
  83  |           await expect(page).toHaveURL(`${config.webUrl}/`);
  84  |           // Pattern 2 — value equality on client state the oracle mandates.
  85  |           expect(
  86  |             await loginPage.storedToken(),
  87  |             'FR-02 requires a successful login to store a JWT client-side',
  88  |           ).toBeTruthy();
  89  |           break;
  90  |         }
  91  | 
  92  |         case 'login-rejected': {
  93  |           // Pattern 3 — visibility.
  94  |           await expect(loginPage.errorMessage).toBeVisible();
  95  |           await expect(page).toHaveURL(/\/login/);
  96  |           expect(
  97  |             await loginPage.storedToken(),
  98  |             'a rejected login must not store a token',
  99  |           ).toBeFalsy();
  100 |           break;
  101 |         }
  102 | 
  103 |         case 'blocked-by-browser-validation': {
  104 |           // Both fields are `required`, so the browser must refuse to submit.
  105 |           const emailValid = await loginPage.emailInput.evaluate(
  106 |             (el) => (el as HTMLInputElement).validity.valid,
  107 |           );
  108 |           const passwordValid = await loginPage.passwordInput.evaluate(
  109 |             (el) => (el as HTMLInputElement).validity.valid,
  110 |           );
  111 |           expect(
  112 |             emailValid && passwordValid,
  113 |             'a required field left blank must fail browser validation',
  114 |           ).toBe(false);
  115 |           await expect(page).toHaveURL(/\/login/);
  116 |           expect(await loginPage.storedToken()).toBeFalsy();
  117 |           break;
  118 |         }
  119 | 
  120 |         case 'email-input-is-type-email': {
  121 |           // Pattern 4 — attribute. The oracle names type="email" explicitly, so
  122 |           // the browser itself must reject a malformed address.
  123 |           await expect(loginPage.emailInput).toHaveAttribute('type', 'email');
  124 |           const valid = await loginPage.emailInput.evaluate(
  125 |             (el) => (el as HTMLInputElement).validity.valid,
  126 |           );
  127 |           expect(valid, 'a malformed email must fail HTML5 validation').toBe(false);
  128 |           await expect(page).toHaveURL(/\/login/);
  129 |           break;
  130 |         }
  131 | 
  132 |         case 'response-excludes-password': {
  133 |           const response = await loginResponse;
  134 |           const body = await response!.text();
  135 |           expect(
  136 |             body,
  137 |             'the login response must return a JWT, not the stored account record',
> 138 |           ).not.toContain('"password"');
      |                 ^ Error: the login response must return a JWT, not the stored account record
  139 |           break;
  140 |         }
  141 | 
  142 |         case 'error-message-is-generic': {
  143 |           // Non-disclosure: a wrong password and an unknown address must be
  144 |           // indistinguishable to the user.
  145 |           const wrongPasswordMessage = await loginPage.errorMessage.innerText();
  146 | 
  147 |           await loginPage.goto();
  148 |           await loginPage.attemptLogin(uniqueEmail('unknown'), 'Whatever!9');
  149 |           const unknownEmailMessage = await loginPage.errorMessage.innerText();
  150 | 
  151 |           // Pattern 5 — equality between two observed strings.
  152 |           expect(
  153 |             wrongPasswordMessage.trim(),
  154 |             'the error must not reveal whether the account exists',
  155 |           ).toBe(unknownEmailMessage.trim());
  156 |           break;
  157 |         }
  158 |       }
  159 |     });
  160 |   }
  161 | });
  162 | 
```