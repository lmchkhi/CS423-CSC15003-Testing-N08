# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: fr03-forgot-reset-password.spec.ts >> FR03-TC-001 registered user can request OTP and reset password through the two-step forgot-password flow
- Location: tests/fr03-forgot-reset-password.spec.ts:177:3

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('body')
Timeout: 1500ms
Expected pattern: /Bước\s*1\s*\/\s*2|Step\s*1\s*\/\s*2/i
Received string:  "
    EShopGiỏ hàngĐăng nhậpĐăng kýQuên Mật KhẩuNhập Email của bạnLấy mã OTP© 2026 EShop SUT. Dành cho mục đích kiểm thử.·········
"

Call log:
  - Expect "soft toContainText" with timeout 1500ms
  - waiting for locator('body')
    15 × locator resolved to <body>…</body>
       - unexpected value "
    EShopGiỏ hàngĐăng nhậpĐăng kýQuên Mật KhẩuNhập Email của bạnLấy mã OTP© 2026 EShop SUT. Dành cho mục đích kiểm thử.
    
  

"

```

```yaml
- banner:
  - link "EShop":
    - /url: /
  - navigation:
    - link "Giỏ hàng":
      - /url: /cart
    - link "Đăng nhập":
      - /url: /login
    - link "Đăng ký":
      - /url: /register
- main:
  - heading "Quên Mật Khẩu" [level=2]
  - text: Nhập Email của bạn
  - textbox
  - button "Lấy mã OTP"
- contentinfo: © 2026 EShop SUT. Dành cho mục đích kiểm thử.
```

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/quay lại đăng nhập|back to login/i)
Expected: visible
Timeout: 1500ms
Error: element(s) not found

Call log:
  - Expect "soft toBeVisible" with timeout 1500ms
  - waiting for getByText(/quay lại đăng nhập|back to login/i)

```

```yaml
- banner:
  - link "EShop":
    - /url: /
  - navigation:
    - link "Giỏ hàng":
      - /url: /cart
    - link "Đăng nhập":
      - /url: /login
    - link "Đăng ký":
      - /url: /register
- main:
  - heading "Quên Mật Khẩu" [level=2]
  - text: Nhập Email của bạn
  - textbox
  - button "Lấy mã OTP"
- contentinfo: © 2026 EShop SUT. Dành cho mục đích kiểm thử.
```

```
Error: FR-03 inherits the email input type requirement from FR-02/FR-22

expect(locator).toHaveAttribute(expected) failed

Locator: locator('form input').first().first()
Expected pattern: /email/i
Received string:  "text"
Timeout: 1500ms

Call log:
  - FR-03 inherits the email input type requirement from FR-02/FR-22 with timeout 1500ms
  - waiting for locator('form input').first().first()
    16 × locator resolved to <input value="" required="" type="text" class="w-full border p-2 rounded"/>
       - unexpected value "text"

```

```yaml
- textbox
```

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('body')
Timeout: 1500ms
Expected pattern: /Bước\s*2\s*\/\s*2|Step\s*2\s*\/\s*2/i
Received string:  "
    EShopGiỏ hàngĐăng nhậpĐăng kýQuên Mật KhẩuMã OTP của bạn là: 8851Mã OTP (4 số)Mật khẩu mớiĐặt lại mật khẩu← Quay lại© 2026 EShop SUT. Dành cho mục đích kiểm thử.·········
"

Call log:
  - Expect "soft toContainText" with timeout 1500ms
  - waiting for locator('body')
    16 × locator resolved to <body>…</body>
       - unexpected value "
    EShopGiỏ hàngĐăng nhậpĐăng kýQuên Mật KhẩuMã OTP của bạn là: 8851Mã OTP (4 số)Mật khẩu mớiĐặt lại mật khẩu← Quay lại© 2026 EShop SUT. Dành cho mục đích kiểm thử.
    
  

"

```

```yaml
- banner:
  - link "EShop":
    - /url: /
  - navigation:
    - link "Giỏ hàng":
      - /url: /cart
    - link "Đăng nhập":
      - /url: /login
    - link "Đăng ký":
      - /url: /register
- main:
  - heading "Quên Mật Khẩu" [level=2]
  - text: "Mã OTP của bạn là: 8851 Mã OTP (4 số)"
  - textbox
  - text: Mật khẩu mới
  - textbox
  - button "Đặt lại mật khẩu"
  - button "← Quay lại"
- contentinfo: © 2026 EShop SUT. Dành cho mục đích kiểm thử.
```

```
Error: demo UI should visibly display the generated 6-digit OTP

expect(locator).toContainText(expected) failed

Locator: locator('body')
Timeout: 1500ms
Expected pattern: /\b\d{6}\b/
Received string:  "
    EShopGiỏ hàngĐăng nhậpĐăng kýQuên Mật KhẩuMã OTP của bạn là: 8851Mã OTP (4 số)Mật khẩu mớiĐặt lại mật khẩu← Quay lại© 2026 EShop SUT. Dành cho mục đích kiểm thử.·········
"

Call log:
  - demo UI should visibly display the generated 6-digit OTP with timeout 1500ms
  - waiting for locator('body')
    16 × locator resolved to <body>…</body>
       - unexpected value "
    EShopGiỏ hàngĐăng nhậpĐăng kýQuên Mật KhẩuMã OTP của bạn là: 8851Mã OTP (4 số)Mật khẩu mớiĐặt lại mật khẩu← Quay lại© 2026 EShop SUT. Dành cho mục đích kiểm thử.
    
  

"

```

```yaml
- banner:
  - link "EShop":
    - /url: /
  - navigation:
    - link "Giỏ hàng":
      - /url: /cart
    - link "Đăng nhập":
      - /url: /login
    - link "Đăng ký":
      - /url: /register
- main:
  - heading "Quên Mật Khẩu" [level=2]
  - text: "Mã OTP của bạn là: 8851 Mã OTP (4 số)"
  - textbox
  - text: Mật khẩu mới
  - textbox
  - button "Đặt lại mật khẩu"
  - button "← Quay lại"
- contentinfo: © 2026 EShop SUT. Dành cho mục đích kiểm thử.
```

```
Error: OTP must be exactly 6 digits

expect(received).toMatch(expected)

Expected pattern: /^\d{6}$/
Received string:  "8851"
```

```
Error: reset step must expose new password and confirm new password fields

expect(received).toBeGreaterThanOrEqual(expected)

Expected: >= 2
Received:    1
```

```
Error: login with the reset password should succeed

expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 401
```

# Test source

```ts
  70  |     [
  71  |       page.getByRole('link', { name: /quên mật khẩu|forgot password/i }),
  72  |       page.getByRole('button', { name: /quên mật khẩu|forgot password/i }),
  73  |       page.getByText(/quên mật khẩu|forgot password/i)
  74  |     ],
  75  |     'forgot password navigation'
  76  |   );
  77  |   await forgotLink.click();
  78  | }
  79  | 
  80  | async function submitCurrentForm(page: Page): Promise<void> {
  81  |   const submit = await firstVisible(
  82  |     [
  83  |       page.getByRole('button', {
  84  |         name: /gửi|lấy mã|tiếp tục|đặt lại|xác nhận|submit|send|reset|continue/i
  85  |       }),
  86  |       page.locator('button[type="submit"], input[type="submit"]')
  87  |     ],
  88  |     'form submit'
  89  |   );
  90  | 
  91  |   await submit.click();
  92  | }
  93  | 
  94  | async function fillForgotPasswordEmail(page: Page, email: string): Promise<void> {
  95  |   const emailInput = await firstVisible(
  96  |     [
  97  |       page.getByLabel(/email|e-mail|địa chỉ email/i),
  98  |       page.getByPlaceholder(/email|e-mail|địa chỉ email/i),
  99  |       page.locator('input[type="email"]'),
  100 |       page.locator('form input').first()
  101 |     ],
  102 |     'registered email input'
  103 |   );
  104 | 
  105 |   await expect.soft(emailInput, 'FR-03 inherits the email input type requirement from FR-02/FR-22').toHaveAttribute(
  106 |     'type',
  107 |     /email/i,
  108 |     { timeout: 1_500 }
  109 |   );
  110 |   await emailInput.fill(email);
  111 |   await expect(emailInput).toHaveValue(email);
  112 | }
  113 | 
  114 | async function extractVisibleOtp(
  115 |   page: Page,
  116 |   expectedOtpPattern: string,
  117 |   otpExtractionPattern: string
  118 | ): Promise<string> {
  119 |   const expectedOtpRegex = new RegExp(expectedOtpPattern);
  120 |   await expect.soft(
  121 |     page.locator('body'),
  122 |     'demo UI should visibly display the generated 6-digit OTP'
  123 |   ).toContainText(expectedOtpRegex, { timeout: 1_500 });
  124 | 
  125 |   const extractionRegex = new RegExp(otpExtractionPattern);
  126 |   await expect(page.locator('body'), 'demo UI should display an OTP value that can be used in step 2').toContainText(
  127 |     extractionRegex
  128 |   );
  129 |   const bodyText = (await page.locator('body').textContent()) ?? '';
  130 |   const match = bodyText.match(extractionRegex);
  131 |   if (!match) {
  132 |     throw new Error('Could not extract a visible OTP from the forgot-password page.');
  133 |   }
  134 |   return match[1] ?? match[0];
  135 | }
  136 | 
  137 | async function fillResetPasswordForm(page: Page, otp: string, newPassword: string): Promise<void> {
  138 |   const otpInput = await firstVisible(
  139 |     [
  140 |       page.getByLabel(/otp|mã|token/i),
  141 |       page.getByPlaceholder(/otp|mã|token/i),
  142 |       page.locator(
  143 |         'input[name*="otp" i], input[name*="token" i], input[autocomplete="one-time-code"], input[type="number"], input[type="text"]'
  144 |       )
  145 |     ],
  146 |     'OTP input'
  147 |   );
  148 |   await otpInput.fill(otp);
  149 |   await expect(otpInput).toHaveValue(otp);
  150 | 
  151 |   const passwordInputs = page.locator('input[type="password"]');
  152 |   const passwordInputCount = await passwordInputs.count();
  153 |   expect
  154 |     .soft(passwordInputCount, 'reset step must expose new password and confirm new password fields')
  155 |     .toBeGreaterThanOrEqual(2);
  156 | 
  157 |   await passwordInputs.nth(0).fill(newPassword);
  158 |   await expect(passwordInputs.nth(0)).toHaveValue(newPassword);
  159 |   if (passwordInputCount >= 2) {
  160 |     await passwordInputs.nth(1).fill(newPassword);
  161 |     await expect(passwordInputs.nth(1)).toHaveValue(newPassword);
  162 |   }
  163 | }
  164 | 
  165 | async function assertLoginWithNewPassword(request: APIRequestContext, email: string, password: string): Promise<void> {
  166 |   const response = await request.post(`${apiBaseUrl}/api/login`, {
  167 |     data: { email, password }
  168 |   });
  169 | 
> 170 |   expect(response.status(), 'login with the reset password should succeed').toBe(200);
      |                                                                             ^ Error: login with the reset password should succeed
  171 |   const body = (await response.json()) as LoginResponse;
  172 |   expect(body, 'login response should include a JWT token').toHaveProperty('token');
  173 |   expect(body.token).toEqual(expect.any(String));
  174 | }
  175 | 
  176 | for (const testCase of loadFr03Cases()) {
  177 |   test(`${testCase.id} ${testCase.title}`, async ({ page, request }) => {
  178 |     const email = uniqueEmail(testCase);
  179 |     await createUser(request, testCase, email);
  180 | 
  181 |     await openForgotPasswordPage(page, testCase);
  182 |     await expect
  183 |       .soft(page.locator('body'))
  184 |       .toContainText(new RegExp(testCase.expectedStepOnePattern, 'i'), { timeout: 1_500 });
  185 |     await expect.soft(page.getByText(/quay lại đăng nhập|back to login/i)).toBeVisible({ timeout: 1_500 });
  186 | 
  187 |     await fillForgotPasswordEmail(page, email);
  188 |     const forgotPasswordResponse = page
  189 |       .waitForResponse((response) => response.url().includes('/api/forgot-password'), { timeout: 7_000 })
  190 |       .catch(() => null);
  191 |     await submitCurrentForm(page);
  192 |     await forgotPasswordResponse;
  193 | 
  194 |     await expect
  195 |       .soft(page.locator('body'))
  196 |       .toContainText(new RegExp(testCase.expectedStepTwoPattern, 'i'), { timeout: 1_500 });
  197 |     const otp = await extractVisibleOtp(page, testCase.expectedOtpPattern, testCase.otpExtractionPattern);
  198 |     expect.soft(otp, 'OTP must be exactly 6 digits').toMatch(/^\d{6}$/);
  199 | 
  200 |     await fillResetPasswordForm(page, otp, testCase.newPassword);
  201 |     const resetPasswordResponse = page
  202 |       .waitForResponse((response) => response.url().includes('/api/reset-password'), { timeout: 7_000 })
  203 |       .catch(() => null);
  204 |     await submitCurrentForm(page);
  205 |     await resetPasswordResponse;
  206 | 
  207 |     await expect(page.locator('body')).toContainText(new RegExp(testCase.expectedSuccessPattern, 'i'));
  208 |     await assertLoginWithNewPassword(request, email, testCase.newPassword);
  209 |   });
  210 | }
  211 | 
```